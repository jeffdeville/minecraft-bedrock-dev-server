# Server runtime state -- never in git
data/
state/
backups/
addons/
.env

# Secrets -- resolved values, rendered by op inject. The template is committed.
.envrc
!.envrc.template

# Editors / OS / tools
.DS_Store
.vscode/
# ...except the learner workspace template, whose editor settings are content
!course/templates/workspace/.vscode/
*.swp
__pycache__/
.serena/
